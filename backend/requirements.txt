# =============================================================================
# ShortLink 后端依赖
# 版本已锁定,升级前请先在本地跑通测试
# =============================================================================

# ---------- Web 框架 ----------
Flask==3.0.3
Werkzeug==3.0.4

# ---------- ORM 与迁移 ----------
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.34
alembic==1.13.2

# ---------- 序列化与校验 ----------
marshmallow==3.22.0
flask-marshmallow==1.3.0
marshmallow-sqlalchemy==1.0.0

# ---------- 接口文档(Swagger) ----------
flasgger==0.9.7.1

# ---------- 环境变量 ----------
python-dotenv==1.0.1

# ---------- MySQL 驱动 ----------
PyMySQL==1.1.1
cryptography==43.0.1

# ---------- Redis 客户端 ----------
redis==5.0.8

# ---------- 生产 WSGI 服务器 ----------
gunicorn==23.0.0
