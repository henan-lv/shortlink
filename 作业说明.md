# 作业二:短链接服务 · 作业说明

> **目标:** 考察系统设计思维和后端工程能力。

---

## 一、作业要求

### 1. 核心 API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/shorten` | 接收一个长链接,返回对应的短链接 |
| `GET` | `/:code` | 访问短码时,**302 重定向**到原始长链接 |

### 2. 技术要求

- 短码长度为 **6 ~ 8 位**,由**字母和数字**组成(Base62)
- 相同的**长链接**应返回相同的短码(**去重**)
- 记录每个短链接的**访问次数**
- `GET /stats/:code`:返回该短链接的访问统计信息

### 3. 数据库

- **自选**,需在文档中说明**选型理由**

### 4. 加分项

- 实现**短链接过期机制**(如 7 天后失效)

---

## 二、考察点

1. **编码算法设计** — 短码生成策略(自增 ID 映射 / 哈希 / 仿射变换?)
2. **数据库索引理解** — 唯一索引、复合索引、覆盖索引
3. **HTTP 状态码运用** — 302 / 404 / 410 / 429
4. **边界情况处理** — 空长链、非法字符、重复请求、并发冲突、过期、恶意访问

---

## 三、本项目选型与实现说明

### 3.1 技术栈选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 语言 | Python 3.11 | 语法简洁、生态成熟、Web / ORM / 校验库齐全 |
| Web 框架 | Flask 3.0 | 轻量、灵活,与 SQLAlchemy / Marshmallow 无缝配合 |
| ORM | SQLAlchemy 2.0 + Flask-Migrate | 类型注解友好(`Mapped`),迁移可审计可回滚 |
| 序列化 | Marshmallow | 入参 / 出参统一校验 |
| 数据库 | **MySQL 5.7 (utf8mb4 / InnoDB)** | 见下文选型理由 |
| 缓存 / 限流 | Redis 7 | 高 QPS、短链缓存、滑动窗口限流天然契合 |
| 前端 | Vue 3 + Vite + Pinia | 组件化、构建快、HMR 体验好 |

### 3.2 数据库选型理由

**为什么选 MySQL 而不是 SQLite / PostgreSQL?**

| 维度 | MySQL 5.7 | SQLite | PostgreSQL |
| --- | --- | --- | --- |
| 并发写 | 强(InnoDB 行锁) | 弱(全库锁) | 强 |
| 大数据量 | 分区表 / 水平扩展成熟 | 单文件,>10w 行性能下降 | 分区表成熟 |
| 运维 | 主流云厂商托管服务齐全 | 文件级,不便集中管理 | 主流支持 |
| 学习成本 | 低,互联网主流 | 低 | 中等 |
| 字符集 | utf8mb4 完整支持 emoji | utf8 缺 emoji | utf8 完整 |

**结论:** MySQL 5.7 是国内云厂商托管最成熟、Web 项目最主流的选型,utf8mb4 + InnoDB 满足:
- 高并发短链跳转写入(`click_logs`)
- 长链 + 域名 + 渠道的复合唯一索引(`url_hash`)
- 索引覆盖统计查询

### 3.3 短码生成算法

**自增 ID → 仿射变换 → Base62**

```
id (自增) → y = (a × x + b) mod 62^L → Base62 encode → 短码
```

- `a = 131`,`b = 577`(可在 `.env` 调整,打散顺序)
- 校验:`gcd(a, 62^L) == 1`,保证双射(短码 → ID 可逆)
- 容量:6 位 ≈ 568 亿,8 位 ≈ 218 万亿

**优势:**
- **可逆**:短码 → ID 无需建索引查表
- **可控**:改 `a` / `b` 就完全打散,防止被爬取猜测下一条
- **定长**:符合 6~8 位要求,且避免哈希冲突带来的额外存储

**为什么不用哈希?**
- MD5/SHA256 截断后仍有极小概率冲突,需要数据库兜底
- 哈希无法反向,无法保证 ID 自增有序性,不便排查

### 3.4 数据库索引设计

```sql
-- 短链表
CREATE TABLE short_links (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,    -- 自增,短码输入
    short_code  VARCHAR(16) NOT NULL UNIQUE,           -- 短码唯一索引
    long_url    VARCHAR(2048) NOT NULL,
    url_hash    CHAR(64)     NOT NULL UNIQUE,          -- sha256(long+domain+channel),去重键
    visit_count INT          NOT NULL DEFAULT 0,       -- 访问次数冗余
    status      VARCHAR(16)  NOT NULL DEFAULT 'enabled',
    expire_at   DATETIME     NULL,                     -- 过期时间
    effective_at DATETIME    NULL,                     -- 生效时间
    click_limit INT          NULL,                     -- 点击上限
    ...
    INDEX idx_status_deleted (status, is_deleted),     -- 列表过滤
    INDEX idx_expire (expire_at),                      -- 过期扫描
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 点击日志
CREATE TABLE click_logs (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    short_link_id BIGINT NOT NULL,
    ip            VARCHAR(45),
    user_agent    VARCHAR(512),
    referer       VARCHAR(512),
    created_at    DATETIME NOT NULL,
    INDEX idx_link_time (short_link_id, created_at),   -- 趋势查询覆盖索引
    INDEX idx_created (created_at)                     -- 清理过期日志
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**索引设计要点:**
- `short_code` 唯一索引 → 跳转 O(log n)
- `url_hash` 唯一索引 → **去重**(相同长链 + 域名 + 渠道直接返回旧短码)
- `(status, is_deleted)` 复合索引 → 列表查询过滤
- `(short_link_id, created_at)` 覆盖索引 → 30 天趋势聚合无需回表

### 3.5 HTTP 状态码

| 场景 | 状态码 |
| --- | --- |
| 短链生成成功 | `200 OK` |
| 短链跳转成功 | **302 Found** |
| 短链不存在 | `404 Not Found` |
| 短链已过期 / 未生效 / 已停用 / 达上限 | `410 Gone` |
| 触发限流 | `429 Too Many Requests` |
| 短链有密码 | `401 Unauthorized`(要求输入密码) |
| 反爬拦截(空 UA / 黑名单) | `403 Forbidden` |
| 入参校验失败 | `400 Bad Request` |
| 服务异常 | `500 Internal Server Error` |

### 3.6 边界情况处理

| 场景 | 处理 |
| --- | --- |
| 空长链 / 非法协议(`javascript:` / `file:`) | Schema 校验 → `400` |
| 超长长链(> 2048 字符) | 数据库列长限制 + 校验 |
| 相同长链重复请求 | `url_hash` 唯一索引 → 直接返回旧短码(幂等) |
| 并发创建同一长链 | 数据库唯一约束兜底,后者 `try/except IntegrityError` 重查返回 |
| 短链已过期 | 跳转时检查 `expire_at` → `410` |
| 短链未到生效时间 | 跳转时检查 `effective_at` → `410` |
| 短链点击上限已满 | 跳转时检查 `visit_count >= click_limit` → `410` |
| 短链被恶意封禁 | `status = 'malicious'` → 全局拦截 |
| 高频访问单链 | Redis 滑动窗口 → `429` |
| 访问密码错误 | bcrypt 校验 → `403` |
| 跨域请求 | `CORS_ORIGINS` 白名单 |
| 服务重启后冷启动 | 短链 Redis 缓存自动回填 |

### 3.7 过期机制(加分项实现)

- 字段:`expire_at DATETIME NULL`(可为永不过期)
- 创建时可指定,默认 NULL
- 跳转时实时校验:`now > expire_at` → `410`
- 数据库索引 `idx_expire` 支持后台扫描过期短链,批量清理 / 归档

---

## 四、API 实际路径对照

> 作业要求路径 vs 本项目实际路径(均为 RESTful 风格,前缀 `/api`)

| 作业要求 | 本项目路径 |
| --- | --- |
| `POST /shorten` | `POST /api/shortlinks` |
| `GET /:code` | `GET /s/{code}`(无需 `/api` 前缀,因为走 nginx 直跳) |
| `GET /stats/:code` | `GET /api/shortlinks/{code}/stats` |

---

## 五、文档导航

- 完整产品需求:[`docs/PRD.md`](docs/PRD.md)
- 详细技术设计:[`docs/design.md`](docs/design.md)
- 项目主页:[`README.md`](README.md)
- 部署说明:[`docker/README.md`](docker/README.md)
