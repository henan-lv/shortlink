# 作业说明 · 短链接服务 ShortLink

> 这是一份从产品需求到技术实现、从本地开发到云端部署的完整短链接平台作业说明,涵盖前后端实现、数据库设计、安全机制与运维部署。

---

## 一、项目概述

**ShortLink** 是一个面向运营 / 推广场景的短链 SaaS 平台,核心解决三个问题:

1. **长链难分享** — 把冗长的 URL 转成 6 位短码,便于短信 / 微信 / 海报投放
2. **投放效果不可见** — 实时统计 PV / UV / 时段趋势 / 来源 / 地域
3. **运营缺乏抓手** — 灵活的访问控制(密码、IP 黑名单、UA 黑名单、Referer 黑名单),支持紧急启停

### 核心特性

| 类别 | 功能 |
| --- | --- |
| 短链生成 | 自增 ID → 仿射变换 → Base62,6 位短码(可配 1-16 位) |
| 生命周期 | 生效时间 / 过期时间 / 点击上限 / 软删除与恢复 |
| 访问控制 | 短链密码、IP/CIDR 白黑名单、UA 黑名单、Referer 黑名单 |
| 统计监控 | PV / UV、24h / 30 天趋势、按来源 / UA / 地域 / 设备拆解、实时近 5 分钟、CSV 导出 |
| 多人协作 | 多用户系统、首注册自动管理员、用户管理(增删改查 / 启停 / 重置密码 / 重置 API Key) |
| 接口文档 | Swagger UI 自动生成,`/apidocs` |
| 部署形态 | 仅打包前后端容器,数据库 / Redis 走云服务,通过 `.env` 注入 |

---

## 二、技术栈

### 后端

| 模块 | 技术 | 版本 |
| --- | --- | --- |
| Web 框架 | Flask | 3.0.3 |
| ORM | SQLAlchemy + Flask-Migrate | 2.0.34 / 4.0.7 |
| 序列化 | Marshmallow | 3.22.0 |
| 接口文档 | Flasgger | 0.9.7.1 |
| 数据库 | MySQL | 5.7+(utf8mb4) |
| 缓存 / 限流 | Redis | 7+ |
| WSGI | Gunicorn | 23.0.0 |
| 驱动 | PyMySQL / redis-py | 1.1.1 / 5.0.8 |

### 前端

| 模块 | 技术 | 版本 |
| --- | --- | --- |
| 框架 | Vue 3 | ^3.5.13 |
| 构建 | Vite | ^5.4.11 |
| 路由 | Vue Router | ^4.4.5 |
| 状态 | Pinia | ^2.2.6 |
| HTTP | Axios | ^1.7.7 |
| 可视化 | ECharts | ^5.5.1 |

### 部署

- 后端容器:`python:3.11-slim` + Gunicorn(国内 apt/pip 镜像加速)
- 前端容器:多阶段 `node:20-alpine` 构建 → `nginx:1.27-alpine` 托管
- 编排:Docker Compose v2,自定义 `shortlink_net` bridge 网络
- 数据库 / Redis:阿里云 / 腾讯云等托管服务,通过 `docker/.env` 注入

---

## 三、目录结构

```
短链服务/
├── backend/                     # Flask 后端
│   ├── app/
│   │   ├── api/                 # 蓝图路由
│   │   │   ├── short_link.py    #   生成 / 跳转
│   │   │   ├── links.py         #   链接管理(CRUD / 启停 / 恢复)
│   │   │   ├── stats.py         #   访问监控
│   │   │   ├── access_rules.py  #   IP/UA/Referer 黑白名单
│   │   │   ├── password.py      #   短链密码访问
│   │   │   ├── auth.py          #   登录 / 注册 / 鉴权
│   │   │   ├── admin_users.py   #   用户管理(管理员)
│   │   │   └── health.py        #   健康检查
│   │   ├── models/              # 数据模型(short_link / click_log / user / access_rule / blacklist)
│   │   ├── schemas/             # Marshmallow 请求 / 响应校验
│   │   ├── services/            # 业务层
│   │   │   ├── short_code.py    #   仿射变换 + Base62 短码算法
│   │   │   ├── short_link.py    #   短链 CRUD
│   │   │   ├── access_control.py#   IP / UA / Referer / CIDR 校验
│   │   │   ├── anti_bot.py      #   UA / Referer / 速率 反爬
│   │   │   ├── rate_limit.py    #   Redis 滑动窗口限流(LUA 原子)
│   │   │   ├── analytics.py     #   统计聚合
│   │   │   ├── password.py      #   短链密码
│   │   │   └── security.py      #   风控 / 恶意拦截
│   │   ├── middleware/          # 鉴权装饰器 / CORS / 全局异常
│   │   ├── utils/               # Base62 / 哈希 / IP / 统一响应
│   │   ├── config.py            # 从 env 加载所有配置
│   │   ├── extensions.py
│   │   └── __init__.py          # create_app 工厂
│   ├── migrations/              # Alembic 迁移(5 个版本)
│   ├── tests/
│   ├── .env.example
│   ├── requirements.txt
│   └── wsgi.py
├── frontend/                    # Vue 3 前端
│   ├── src/
│   │   ├── api/                 # axios 请求 + 拦截器
│   │   ├── views/               # 7 个页面:Login / Generate / Links / Stats / StatsDetail / Password / Users
│   │   ├── stores/              # Pinia(auth)
│   │   ├── router/              # Vue Router + adminOnly 守卫
│   │   ├── components/          # 通用组件
│   │   ├── composables/         # 可复用逻辑
│   │   ├── styles/
│   │   └── App.vue              # 全局壳(导航 + 主题)
│   ├── public/favicon.svg       # 网站图标(链环 + 蓝紫渐变)
│   ├── vite.config.js
│   └── package.json
├── docker/                      # 云服务部署包
│   ├── docker-compose.yaml      # 编排:backend + frontend,shortlink_net 网络
│   ├── Dockerfile.backend       # Python + Gunicorn(国内镜像源)
│   ├── Dockerfile.frontend      # 多阶段 Node → Nginx(国内镜像源)
│   ├── nginx.conf               # 反向代理 + SPA
│   ├── .env.example
│   └── README.md
├── deploy/                      # 本地全栈(含 MySQL / Redis 容器)
├── docs/
│   ├── PRD.md                   # 产品需求
│   └── design.md                # 技术设计(架构 / 模块 / 安全 / 性能)
├── README.md
├── 作业说明.md                  # 本文档
└── .gitignore
```

---

## 四、本地开发

### 1. 环境要求

- Python ≥ 3.11
- Node.js ≥ 18
- MySQL 5.7+(可用 `deploy/docker-compose.dev.yml` 起容器)
- Redis 7+

### 2. 启动 MySQL + Redis

```bash
cd deploy
docker compose -f docker-compose.dev.yml up -d mysql redis
```

### 3. 启动后端

```bash
cd backend
cp .env.example .env
# 编辑 .env:DATABASE_URL / REDIS_URL / SECRET_KEY / BASE_DOMAIN ...

pip install -r requirements.txt
FLASK_APP=wsgi.py flask db upgrade
python wsgi.py    # http://127.0.0.1:5000
```

Swagger:`http://127.0.0.1:5000/apidocs`

### 4. 启动前端

```bash
cd frontend
npm install
npm run dev       # http://127.0.0.1:5173
```

第一个注册的用户自动成为管理员。

---

## 五、云服务部署

详见 `docker/README.md`。

### 速查

```bash
cd docker
cp .env.example .env
vim .env   # 必填:SECRET_KEY / DB_* / REDIS_* / BASE_DOMAIN / CORS_ORIGINS

# 容器外跑迁移
cd ../backend
FLASK_APP=wsgi.py flask db upgrade

# 起容器
cd ../docker
docker compose -p shortlink up -d --build

# 验证
curl http://<IP>:53607/healthz
docker compose -p shortlink ps
```

### 网络拓扑

```
浏览器 (https://shortlink.lvhn.top)
  ↓ HTTPS
云 LB / Nginx
  ↓
shortlink-frontend (nginx :80, 映射外网 :53607)
  ├─ /         →  SPA 静态
  ├─ /assets/* →  静态资源
  ├─ /api/*  ─┐
  └─ /s/*    ─┼→  shortlink-backend (gunicorn :5000)
  │           │       ├─→ 云 MySQL
  │           │       └─→ 云 Redis
  │
  └─ (docker 网络 shortlink_net)
```

---

## 六、关键设计

### 6.1 短码生成算法

```
id (自增) → 仿射变换 y = (a*x + b) mod 62^L → Base62 编码 → 6 位短码
```

- `a = 131`,`b = 577`(可改,打散顺序)
- 校验:`gcd(a, 62^L) == 1`,保证双射
- 容量:6 位 ≈ 568 亿,8 位 ≈ 218 万亿
- 可逆:短码 → ID,逆变换用扩展欧几里得求模逆元

### 6.2 访问控制优先级

```
白名单(whitelist) > 黑名单 > 默认放行
```

### 6.3 鉴权

- JWT(HS256),过期 7 天
- `@login_required` / `@admin_required` 双层装饰器
- 首注册自动 `is_admin=1`,后续注册默认 `is_admin=0`

### 6.4 限流

- 全局:Redis 滑动窗口,LUA 脚本保证原子性
- 单链:可独立配置

---

## 七、安全清单

| 项 | 实现 |
| --- | --- |
| 用户密码 | bcrypt(Werkzeug) |
| 短链密码 | bcrypt + 内存校验 |
| 传输 | HTTPS(由云 LB 终结) |
| CORS | 白名单(env 注入) |
| 限流 | Redis 滑动窗口 |
| 反爬 | UA 黑名单 + Referer 黑名单 |
| 密钥 | `.env` 注入 + `.gitignore` 已排除 |
| SQL 注入 | 全 ORM 参数化 |
| XSS | JSON 响应 + 前端 Vue 模板自动转义 |

---

## 八、后续可扩展点

| 优先级 | 内容 |
| --- | --- |
| P1 | 短链 AB 测试(同一长链多短码按权重分流) |
| P1 | 短链二维码 + 海报生成 |
| P1 | Webhook:访问触发企业微信 / 飞书 / 钉钉回调 |
| P2 | 多租户:团队空间 + 独立域名 + 独立统计 |
| P2 | OpenAPI 3.0 文档 |
| P3 | AI 恶意链接识别 + 长链标题智能提取 |

---

## 九、文档导航

- 产品需求:[`docs/PRD.md`](docs/PRD.md)
- 技术设计:[`docs/design.md`](docs/design.md)
- 部署说明:[`docker/README.md`](docker/README.md)
- 项目主页:[`README.md`](README.md)
